export function PlaybooksPage(root){
  root.innerHTML = `
    <div class="card">
      <h3>Playbooks</h3>
      <p class="tag">Setups</p>
      <p class="tag">Risk Rules</p>
      <p class="tag">Mindset</p>
      <p style="color:var(--muted)">Each playbook: checklist + examples + link recent trades.</p>
    </div>`;
}
