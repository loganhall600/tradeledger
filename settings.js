export function SettingsPage(root){
  root.innerHTML = `
    <div class="card">
      <h3>Settings</h3>
      <p style="color:var(--muted)">Theme, fields, tags, currency, data import/export.</p>
    </div>`;
}
